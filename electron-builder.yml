appId: com.chabra.painel-sst
productName: "Painel SST"
copyright: "Copyright © 2025 Chabra"

directories:
  output: dist
  buildResources: electron/assets

icon: electron/assets/icon.png

# Arquivos empacotados no app.asar (código Electron compilado)
files:
  - electron-dist/**/*
  - package.json
  - "!node_modules/.cache/**"
  - "!**/*.map"
  - "!electron/**"
  - "!src/**"

# Recursos copiados para resources/ FORA do asar.
# O servidor Next.js (server.js) precisa estar fora do asar para ser executado
# via spawn + ELECTRON_RUN_AS_NODE=1 (Node puro sem patching de asar).
# Layout: resources/.next/standalone/ com static e public na posição que o server.js espera.
extraResources:
  - from: .next/standalone
    to: .next/standalone
  - from: .next/static
    to: .next/standalone/.next/static
  - from: public
    to: .next/standalone/public
  # Helper nativo de biometria (EPI). Empacota o EXE + DLLs se existirem na pasta.
  # Sem o EXE, a biometria degrada para assinatura por desenho (não quebra o build).
  - from: native/EpiFingerprint
    to: native/EpiFingerprint
    filter:
      - "**/*.exe"
      - "**/*.dll"

win:
  target:
    - target: nsis
      arch:
        - x64
  requestedExecutionLevel: asInvoker

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: "Painel SST"

publish:
  provider: github
  owner: joaojefferson-hash
  repo: Painel-SST--Chabra
  releaseType: release
